{# templates/reservation/front_new_reservation_local.html.twig
   ─ FRONT OFFICE : Nouvelle réservation (local ou salle)
   ─ Route : reservation_local_front_new
   ─ Navbar & Footer : identique reservation index (sticky, locaux / salles / mes réservations + user pill)
#}
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Nouvelle réservation — MindAura</title>
  <link rel="icon" type="image/png" href="{{ asset('images/logo.png') }}"/>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Playfair+Display:ital,wght@0,700;1,400&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --g1: #1B4332; --g2: #2D6A4F; --g3: #52B788;
      --g-pale: #D8F3DC; --g-hover: #E8F5EE;
      --violet: #7B5EA7; --vio-pale: #EDE9F6;
      --red: #DC2626; --red-pale: #FEF2F2;
      --bg: #F0F4F8; --white: #FFFFFF;
      --text: #1A1A2E; --text-sec: #5A6475; --border: #D5DCE6;
      --radius-xl: 24px; --radius-lg: 18px; --radius-md: 12px;
      --ease: cubic-bezier(.22,1,.36,1);
      --shadow: 0 18px 50px rgba(27,67,50,.08);
    }
    html { scroll-behavior: smooth; }
    body { min-height: 100vh; font-family: 'Outfit', sans-serif; background: linear-gradient(180deg, #F7FBF8 0%, #E9F2ED 100%); color: var(--text); overflow-x: hidden; line-height: 1.6; }

    /* ══ NAVBAR (identique reservation index) ══ */
    .nav {
      position: sticky; top: 0; z-index: 999;
      background: rgba(255,255,255,.93);
      backdrop-filter: blur(18px) saturate(150%);
      border-bottom: 1px solid rgba(27,67,50,.09);
      padding: 0 48px;
      display: flex; align-items: center; gap: 20px;
      height: 68px;
      transition: box-shadow .3s;
    }
    .nav.scrolled { box-shadow: 0 4px 24px rgba(27,67,50,.11); }
    .nav-brand { display: flex; align-items: center; gap: 11px; text-decoration: none; flex-shrink: 0; }
    .nav-brand-icon { width: 42px; height: 42px; border-radius: 12px; background: var(--g1); display: grid; place-items: center; box-shadow: 0 4px 14px rgba(27,67,50,.28); transition: transform .3s var(--ease); overflow: hidden; }
    .nav-brand:hover .nav-brand-icon { transform: rotate(-6deg) scale(1.05); }
    .nav-brand-name { font-family: 'Playfair Display', serif; font-size: 1.22rem; font-weight: 700; color: var(--text); line-height: 1.1; }
    .nav-brand-sub  { font-size: .59rem; color: var(--text-sec); letter-spacing: 1.2px; text-transform: uppercase; font-weight: 600; }
    .nav-links { flex: 1; display: flex; align-items: center; gap: 2px; justify-content: center; }
    .nav-link { padding: 7px 17px; border-radius: 99px; font-size: .87rem; font-weight: 500; color: var(--text-sec); text-decoration: none; transition: all .25s var(--ease); }
    .nav-link:hover  { background: var(--g-hover); color: var(--g1); }
    .nav-link.active { background: var(--g-pale); color: var(--g1); font-weight: 700; }
    .nav-right { display: flex; align-items: center; gap: 10px; }
    .nav-user-pill-wrapper { position: relative; }
    .nav-user-pill { display: flex; align-items: center; gap: 9px; padding: 5px 14px 5px 5px; border-radius: 99px; background: var(--bg); border: 1.5px solid rgba(27,67,50,.13); cursor: pointer; font-family: 'Outfit', sans-serif; transition: all .25s var(--ease); }
    .nav-user-pill:hover, .nav-user-pill[aria-expanded="true"] { background: var(--g-hover); border-color: var(--g3); box-shadow: 0 0 0 3px rgba(82,183,136,.12); }
    .nav-avatar { width: 32px; height: 32px; border-radius: 50%; background: var(--g-pale); border: 2px solid var(--g3); display: grid; place-items: center; font-size: .77rem; font-weight: 700; color: var(--g1); overflow: hidden; flex-shrink: 0; }
    .nav-avatar img { width: 100%; height: 100%; object-fit: cover; }
    .nav-user-name  { font-size: .83rem; font-weight: 600; color: var(--text); }
    .nav-role-badge { font-size: .7rem; color: var(--text-sec); background: var(--g-pale); padding: 2px 8px; border-radius: 99px; font-weight: 600; }
    .pill-chevron { width: 14px; height: 14px; stroke: var(--text-sec); transition: transform .3s var(--ease); margin-left: 2px; fill: none; stroke-width: 2; }
    .nav-user-pill[aria-expanded="true"] .pill-chevron { transform: rotate(180deg); }
    .user-dropdown { position: absolute; top: calc(100% + 10px); right: 0; width: 290px; background: var(--white); border: 1.5px solid var(--border); border-radius: var(--radius-lg); box-shadow: 0 20px 48px rgba(27,67,50,.14), 0 4px 12px rgba(27,67,50,.07); z-index: 1000; opacity: 0; visibility: hidden; transform: translateY(-8px) scale(.97); transform-origin: top right; transition: opacity .22s var(--ease), transform .22s var(--ease), visibility .22s; overflow: hidden; }
    .user-dropdown.open { opacity: 1; visibility: visible; transform: translateY(0) scale(1); }
    .dropdown-header { display: flex; align-items: center; gap: 12px; padding: 16px 18px 14px; background: linear-gradient(135deg, var(--g-pale) 0%, rgba(255,255,255,0) 100%); }
    .dropdown-avatar { width: 44px; height: 44px; border-radius: 50%; background: linear-gradient(135deg, var(--g2), var(--g3)); border: 2.5px solid var(--g3); display: grid; place-items: center; font-size: .9rem; font-weight: 700; color: #fff; overflow: hidden; flex-shrink: 0; box-shadow: 0 3px 10px rgba(27,67,50,.2); }
    .dropdown-avatar img { width: 100%; height: 100%; object-fit: cover; }
    .dropdown-user-name  { font-size: .9rem; font-weight: 700; color: var(--text); line-height: 1.2; }
    .dropdown-user-email { font-size: .74rem; color: var(--text-sec); margin-top: 2px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 180px; }
    .dropdown-divider    { height: 1px; background: var(--border); margin: 4px 0; }
    .dropdown-item { display: flex; align-items: center; gap: 12px; padding: 11px 18px; text-decoration: none; color: var(--text); transition: background .18s var(--ease); }
    .dropdown-item:hover { background: var(--g-hover); }
    .dropdown-item-icon { width: 36px; height: 36px; border-radius: 10px; flex-shrink: 0; background: var(--g-pale); display: grid; place-items: center; transition: transform .2s var(--ease); }
    .dropdown-item-icon svg { width: 17px; height: 17px; stroke: var(--g1); fill: none; stroke-width: 1.8; }
    .dropdown-item:hover .dropdown-item-icon { transform: scale(1.08); }
    .dropdown-item-icon--violet { background: var(--vio-pale); }
    .dropdown-item-icon--violet svg { stroke: var(--violet); }
    .dropdown-item-icon--red { background: #FEF2F2; }
    .dropdown-item-icon--red svg { stroke: var(--red); }
    .dropdown-item-text { flex: 1; display: flex; flex-direction: column; gap: 1px; }
    .dropdown-item-label { font-size: .87rem; font-weight: 600; }
    .dropdown-item-sub   { font-size: .73rem; color: var(--text-sec); }
    .dropdown-item-arrow { width: 14px; height: 14px; fill: none; stroke: var(--border); stroke-width: 2; transition: stroke .2s, transform .2s var(--ease); }
    .dropdown-item:hover .dropdown-item-arrow { stroke: var(--g3); transform: translateX(3px); }
    .dropdown-item--danger .dropdown-item-label { color: var(--red); }
    .dropdown-item--danger:hover { background: #FEF2F2; }

    /* PAGE CONTENT */
    .page-shell { max-width: 1220px; margin: 0 auto; padding: 40px 48px 0; }

    /* HERO BANNER */
    .hero { display: grid; grid-template-columns: 1fr 280px; gap: 24px; align-items: start; margin-bottom: 36px; }
    .hero-card { padding: 38px 36px; border-radius: var(--radius-xl); background: linear-gradient(135deg, rgba(27,67,50,.97), rgba(82,183,136,.92)); color: white; box-shadow: var(--shadow); position: relative; overflow: hidden; }
    .hero-card::before { content: ''; position: absolute; top: -20px; right: -40px; width: 200px; height: 200px; background: rgba(255,255,255,.08); border-radius: 50%; }
    .hero-card::after  { content: ''; position: absolute; bottom: -30px; left: -20px; width: 140px; height: 140px; background: rgba(255,255,255,.04); border-radius: 50%; }
    .hero-title { font-family: 'Playfair Display', serif; font-size: 2.5rem; line-height: 1.1; margin-bottom: 14px; position: relative; }
    .hero-copy { font-size: .97rem; line-height: 1.8; color: rgba(255,255,255,.88); max-width: 540px; position: relative; }
    .hero-badges { display: flex; flex-direction: column; gap: 10px; margin-top: 28px; position: relative; }
    .hero-badge-item { display: inline-flex; align-items: center; gap: 10px; padding: 12px 16px; border-radius: 14px; background: rgba(255,255,255,.12); font-weight: 600; font-size: .88rem; border: 1px solid rgba(255,255,255,.1); }

    .tips-card { background: var(--white); border-radius: var(--radius-xl); box-shadow: var(--shadow); border: 1px solid rgba(27,67,50,.06); padding: 28px; }
    .tips-title { font-family: 'Playfair Display', serif; font-size: 1.1rem; color: var(--g1); margin-bottom: 10px; }
    .tips-sub   { font-size: .85rem; color: var(--text-sec); line-height: 1.7; margin-bottom: 18px; }
    .tip-item { border-radius: 14px; padding: 14px 16px; font-size: .85rem; line-height: 1.6; margin-bottom: 10px; }
    .tip-item:last-child { margin-bottom: 0; }

    /* FORM PANEL */
    .panel { display: grid; grid-template-columns: 1.5fr 0.9fr; gap: 28px; }
    .panel-card { background: white; border-radius: var(--radius-xl); box-shadow: var(--shadow); border: 1px solid rgba(15,23,42,.06); padding: 36px; }
    .section-title { display: flex; align-items: center; gap: 12px; font-family: 'Playfair Display', serif; font-size: 1.35rem; margin-bottom: 10px; color: var(--g1); }
    .section-subtitle { margin-bottom: 28px; color: var(--text-sec); line-height: 1.75; font-size: .9rem; }

    .field-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 18px; }
    .field-full { grid-column: 1/-1; }
    .field-block { display: grid; gap: 7px; }
    .field-label { font-size: .8rem; font-weight: 700; color: var(--text-sec); text-transform: uppercase; letter-spacing: .5px; }

    .field-block input,
    .field-block select,
    .field-block textarea {
      width: 100%; border-radius: 14px; border: 1.5px solid var(--border);
      background: var(--bg); padding: 13px 16px; font-size: .95rem;
      color: var(--text); outline: none; font-family: 'Outfit', sans-serif;
      transition: all .2s var(--ease);
    }
    .field-block input:focus,
    .field-block select:focus,
    .field-block textarea:focus { border-color: var(--g2); box-shadow: 0 0 0 4px rgba(82,183,136,.12); background: #fff; }
    .field-block input[readonly],
    .field-block textarea[readonly] { background: #F5FAF6; color: var(--g1); font-weight: 600; border-style: dashed; }

    .form-note { font-size: .78rem; color: var(--text-sec); display: flex; align-items: center; gap: 5px; }
    .form-note::before { content: 'ℹ️'; }

    .alert { padding: 16px 18px; border-radius: 14px; background: #FEF7F2; border: 1px solid #FBCBBB; color: #7C2D12; margin-bottom: 18px; font-size: .88rem; }

    .actions { display: flex; gap: 14px; flex-wrap: wrap; margin-top: 28px; }
    .btn-primary { display: inline-flex; align-items: center; justify-content: center; gap: 9px; padding: 14px 28px; border-radius: 14px; border: none; background: var(--g1); color: white; font-size: .95rem; font-weight: 700; text-decoration: none; cursor: pointer; font-family: 'Outfit', sans-serif; transition: all .25s var(--ease); }
    .btn-primary:hover { background: var(--g2); transform: translateY(-2px); box-shadow: 0 10px 28px rgba(27,67,50,.25); }
    .btn-secondary { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 14px 24px; border-radius: 14px; border: 1.5px solid var(--border); background: #fff; color: var(--text); font-size: .95rem; font-weight: 700; text-decoration: none; font-family: 'Outfit', sans-serif; transition: all .25s var(--ease); }
    .btn-secondary:hover { background: var(--g-pale); border-color: var(--g3); color: var(--g1); }

    /* RECAP SIDEBAR */
    .recap-item { border-radius: 16px; padding: 16px 18px; margin-bottom: 12px; }
    .recap-item:last-child { margin-bottom: 0; }
    .recap-label { font-size: .72rem; font-weight: 700; text-transform: uppercase; letter-spacing: .7px; color: var(--text-sec); margin-bottom: 4px; }
    .recap-value { font-size: .95rem; font-weight: 700; color: var(--g1); }

    /* ══ FOOTER (identique reservation index) ══ */
    .footer { background: var(--g1); padding: 60px 48px 30px; margin-top: 60px; }
    .footer-inner { max-width: 1200px; margin: 0 auto; }
    .footer-top { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 44px; margin-bottom: 50px; }
    .footer-brand-name { font-family: 'Playfair Display', serif; font-size: 1.38rem; color: #fff; }
    .footer-brand-sub  { font-size: .59rem; color: rgba(255,255,255,.45); letter-spacing: 1.1px; text-transform: uppercase; font-weight: 600; }
    .footer-tagline    { font-size: .85rem; color: rgba(255,255,255,.52); line-height: 1.72; margin-top: 14px; max-width: 265px; }
    .footer-socials { display: flex; gap: 9px; margin-top: 20px; }
    .f-social { width: 36px; height: 36px; border-radius: 9px; background: rgba(255,255,255,.08); border: 1px solid rgba(255,255,255,.12); display: grid; place-items: center; text-decoration: none; transition: all .25s; }
    .f-social:hover { background: var(--g2); border-color: var(--g2); }
    .f-social svg { width: 16px; height: 16px; stroke: rgba(255,255,255,.65); fill: none; stroke-width: 1.8; }
    .footer-col-title { font-size: .73rem; font-weight: 700; letter-spacing: 1.2px; text-transform: uppercase; color: rgba(255,255,255,.9); margin-bottom: 16px; }
    .footer-link { display: block; font-size: .84rem; color: rgba(255,255,255,.52); text-decoration: none; padding: 4px 0; transition: color .22s; }
    .footer-link:hover { color: #fff; }
    .footer-bottom { border-top: 1px solid rgba(255,255,255,.1); padding-top: 26px; display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 10px; }
    .footer-copy  { font-size: .78rem; color: rgba(255,255,255,.37); }
    .footer-heart { font-size: .72rem; color: rgba(255,255,255,.37); }
    .footer-heart span { color: var(--g3); }

    /* TOAST */
    .toast { position: fixed; bottom: 30px; right: 30px; z-index: 9999; background: var(--g1); color: #fff; padding: 14px 22px; border-radius: 13px; font-size: .88rem; font-weight: 600; box-shadow: 0 8px 28px rgba(27,67,50,.32); display: flex; align-items: center; gap: 10px; transform: translateY(80px); opacity: 0; transition: all .4s var(--ease); pointer-events: none; }
    .toast.show { transform: none; opacity: 1; }

    @media(max-width:1024px) { .footer-top { grid-template-columns: 1fr 1fr; } .hero { grid-template-columns: 1fr; } .panel { grid-template-columns: 1fr; } }
    @media(max-width:768px) { .nav { padding: 0 20px; } .nav-links { display: none; } .page-shell { padding: 24px 20px 0; } .footer { padding: 50px 24px 24px; } .footer-top { grid-template-columns: 1fr; gap: 28px; } }
    @media(max-width:640px) { .field-grid { grid-template-columns: 1fr; } .field-full { grid-column: 1; } }
  </style>
</head>
<body>

{# ══ Variables rôle ══ #}
{% set user      = app.user %}
{% set roleRaw   = user.roleUtilisateur %}
{% set roleSlug  = roleRaw|replace({'ROLE_': ''})|lower %}
{% set roleLabel = {'patient':'Patient','medecin':'Médecin','coach':'Coach','psychologue':'Psychologue','user':'Utilisateur','admin':'Admin'}[roleSlug] ?? (roleSlug|capitalize) %}
{% set roleEmoji = {'patient':'🧠','medecin':'🩺','coach':'💪','psychologue':'🧘','user':'👤','admin':'🛡️'}[roleSlug] ?? '👤' %}

{# ══ NAVBAR ══ #}
<nav class="nav" id="main-nav">
  <a href="{{ path('app_home') }}" class="nav-brand">
    <div class="nav-brand-icon">
      <img src="{{ asset('images/logo.png') }}" alt="MindAura Logo" style="width:100%;height:100%;object-fit:contain;border-radius:12px;">
    </div>
    <div>
      <div class="nav-brand-name">MindAura</div>
      <div class="nav-brand-sub">Bien-être &amp; Sérénité</div>
    </div>
  </a>

  <div class="nav-links">
    <a href="{{ path('locaux_psychiatrie_front_locaux_index') }}" class="nav-link">Locaux</a>
    <a href="{{ path('salle_front_salles_index') }}"              class="nav-link">Salles</a>
    <a href="{{ path('reservation_local_front_index') }}"         class="nav-link active">Mes réservations</a>
  </div>

  <div class="nav-right">
    <div class="nav-user-pill-wrapper" id="userPillWrapper">
      <button class="nav-user-pill" id="userPillBtn" type="button" aria-haspopup="true" aria-expanded="false">
        <div class="nav-avatar">
          {% if user.photoProfilUtilisateur is defined and user.photoProfilUtilisateur != 'default.png' %}
            <img src="{{ asset('avatars/' ~ user.photoProfilUtilisateur) }}" alt="avatar"/>
          {% else %}
            {{ user.prenomUtilisateur|first|upper }}{{ user.nomUtilisateur|first|upper }}
          {% endif %}
        </div>
        <span class="nav-user-name">{{ user.prenomUtilisateur }}</span>
        <span class="nav-role-badge">{{ roleEmoji }} {{ roleLabel }}</span>
        <svg class="pill-chevron" viewBox="0 0 16 16" fill="none" stroke-linecap="round" stroke-linejoin="round">
          <path d="M4 6l4 4 4-4"/>
        </svg>
      </button>

      <div class="user-dropdown" id="userDropdown" role="menu">
        <div class="dropdown-header">
          <div class="dropdown-avatar">
            {% if user.photoProfilUtilisateur is defined and user.photoProfilUtilisateur != 'default.png' %}
              <img src="{{ asset('avatars/' ~ user.photoProfilUtilisateur) }}" alt="avatar"/>
            {% else %}
              {{ user.prenomUtilisateur|first|upper }}{{ user.nomUtilisateur|first|upper }}
            {% endif %}
          </div>
          <div>
            <div class="dropdown-user-name">{{ user.prenomUtilisateur }} {{ user.nomUtilisateur }}</div>
            <div class="dropdown-user-email">{{ user.emailUtilisateur }}</div>
          </div>
        </div>
        <div class="dropdown-divider"></div>
        <a href="{{ path('app_profil') }}" class="dropdown-item" role="menuitem">
          <span class="dropdown-item-icon"><svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="10" cy="7" r="3.5"/><path d="M3 18c0-3.9 3.1-7 7-7s7 3.1 7 7" stroke-linecap="round"/></svg></span>
          <span class="dropdown-item-text"><span class="dropdown-item-label">Voir mon profil</span><span class="dropdown-item-sub">Consulter mes informations</span></span>
          <svg class="dropdown-item-arrow" viewBox="0 0 16 16"><path d="M6 4l4 4-4 4" stroke-linecap="round"/></svg>
        </a>
        <a href="{{ path('app_profil_edit') }}" class="dropdown-item" role="menuitem">
          <span class="dropdown-item-icon dropdown-item-icon--violet"><svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M13.586 3.586a2 2 0 112.828 2.828l-8.5 8.5L4 16l1.086-3.914 8.5-8.5z" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
          <span class="dropdown-item-text"><span class="dropdown-item-label">Modifier mon profil</span><span class="dropdown-item-sub">Changer mes informations</span></span>
          <svg class="dropdown-item-arrow" viewBox="0 0 16 16"><path d="M6 4l4 4-4 4" stroke-linecap="round"/></svg>
        </a>
        <div class="dropdown-divider"></div>
        <a href="{{ path('app_logout') }}" class="dropdown-item dropdown-item--danger" role="menuitem">
          <span class="dropdown-item-icon dropdown-item-icon--red"><svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M13 3h4v14h-4M9 14l4-4-4-4M13 10H3" stroke-linecap="round" stroke-linejoin="round"/></svg></span>
          <span class="dropdown-item-text"><span class="dropdown-item-label">Se déconnecter</span><span class="dropdown-item-sub">Quitter la session</span></span>
        </a>
      </div>
    </div>
  </div>
</nav>

<div class="page-shell">

  {# ── HERO ── #}
  <div class="hero">
    <div class="hero-card">
      <div class="hero-title">Nouvelle réservation</div>
      <p class="hero-copy">Complétez votre demande de réservation. Vos nom et prénom sont pré-remplis depuis votre profil. Le prix se calcule automatiquement en fonction de la durée choisie.</p>
      <div class="hero-badges">
        <span class="hero-badge-item">✅ Profil patient lié</span>
        <span class="hero-badge-item">📌 Local ou salle au choix</span>
        <span class="hero-badge-item">⏱️ Prix calculé automatiquement</span>
      </div>
    </div>
    <div class="tips-card">
      <div class="tips-title">💡 Conseils rapides</div>
      <p class="tips-sub">Sélectionnez la salle ou le local souhaité, choisissez date et horaires, puis validez.</p>
      <div class="tip-item" style="background:#F0F6F0;">🚪 Sélectionnez d'abord la <strong>salle</strong> si vous réservez une consultation.</div>
      <div class="tip-item" style="background:#EFFAF4;">✔️ Vérifiez la <strong>disponibilité</strong> avant de réserver.</div>
      <div class="tip-item" style="background:#EBF4FF;">💶 Le <strong>prix s'ajuste</strong> selon la durée de la séance.</div>
    </div>
  </div>

  {# ── PANEL FORM + RECAP ── #}
  <div class="panel">
    <section class="panel-card">
      <div class="section-title">📋 Formulaire de réservation</div>
      <p class="section-subtitle">Le formulaire est pré-rempli avec votre identité. Modifiez la salle ou le local si besoin.</p>

      {% if form.vars.errors|length > 0 %}
        <div class="alert">⚠️ Certaines informations sont invalides. Veuillez vérifier les champs signalés.</div>
      {% endif %}

      {{ form_start(form, {attr: {novalidate:'novalidate'}}) }}

      <div class="field-grid">

        <div class="field-block field-full">
          <label class="field-label">{{ form_label(form.local) }}</label>
          {{ form_widget(form.local, {attr: {style:'min-height:48px;border-radius:14px;border:1.5px solid var(--border);padding:12px 14px;font-family:Outfit,system-ui,sans-serif;cursor:pointer;background:var(--bg);width:100%;'}}) }}
          {% for error in form.local.vars.errors %}<div class="alert">{{ error.message }}</div>{% endfor %}
        </div>

        <div class="field-block field-full">
          <label class="field-label">{{ form_label(form.salle) }}</label>
          {{ form_widget(form.salle, {attr: {style:'min-height:48px;border-radius:14px;border:1.5px solid var(--border);padding:12px 14px;font-family:Outfit,system-ui,sans-serif;cursor:pointer;background:var(--bg);width:100%;'}}) }}
          {% for error in form.salle.vars.errors %}<div class="alert">{{ error.message }}</div>{% endfor %}
        </div>

        <div class="field-block">
          <label class="field-label">{{ form_label(form.prenomCl) }}</label>
          {{ form_widget(form.prenomCl, {attr: {readonly:'readonly', style:'border-radius:14px;border:1.5px dashed var(--border);padding:13px 14px;font-family:Outfit,system-ui,sans-serif;background:#F5FAF6;color:var(--g1);font-weight:600;width:100%;'}}) }}
          <span class="form-note">Prénom chargé depuis votre profil.</span>
        </div>

        <div class="field-block">
          <label class="field-label">{{ form_label(form.nomCl) }}</label>
          {{ form_widget(form.nomCl, {attr: {readonly:'readonly', style:'border-radius:14px;border:1.5px dashed var(--border);padding:13px 14px;font-family:Outfit,system-ui,sans-serif;background:#F5FAF6;color:var(--g1);font-weight:600;width:100%;'}}) }}
          <span class="form-note">Nom chargé depuis votre profil.</span>
        </div>

        <div class="field-block">
          <label class="field-label">{{ form_label(form.dateReservation) }}</label>
          {{ form_widget(form.dateReservation, {attr: {style:'border-radius:14px;border:1.5px solid var(--border);padding:13px 14px;font-family:Outfit,system-ui,sans-serif;background:var(--bg);width:100%;'}}) }}
          {% for error in form.dateReservation.vars.errors %}<div class="alert">{{ error.message }}</div>{% endfor %}
        </div>

        <div class="field-block">
          {# spacer on mobile — empty #}
        </div>

        <div class="field-block">
          <label class="field-label">{{ form_label(form.heureDebutReservation) }}</label>
          {{ form_widget(form.heureDebutReservation, {attr: {style:'border-radius:14px;border:1.5px solid var(--border);padding:13px 14px;font-family:Outfit,system-ui,sans-serif;background:var(--bg);width:100%;'}}) }}
          {% for error in form.heureDebutReservation.vars.errors %}<div class="alert">{{ error.message }}</div>{% endfor %}
        </div>

        <div class="field-block">
          <label class="field-label">{{ form_label(form.heureFinReservation) }}</label>
          {{ form_widget(form.heureFinReservation, {attr: {style:'border-radius:14px;border:1.5px solid var(--border);padding:13px 14px;font-family:Outfit,system-ui,sans-serif;background:var(--bg);width:100%;'}}) }}
          {% for error in form.heureFinReservation.vars.errors %}<div class="alert">{{ error.message }}</div>{% endfor %}
        </div>

        <div class="field-block field-full">
          <label class="field-label">{{ form_label(form.prixReservation) }}</label>
          {{ form_widget(form.prixReservation, {attr: {readonly:'readonly', style:'border-radius:14px;border:1.5px dashed var(--border);padding:13px 14px;font-family:Outfit,system-ui,sans-serif;background:#F0F8F2;color:var(--g1);font-weight:700;font-size:1.05rem;width:100%;'}}) }}
          <span class="form-note">Calculé automatiquement selon la durée. Base : 80 DT/h + 20 DT par tranche de 30 min supplémentaires.</span>
        </div>

        <div class="field-block field-full">
          <label class="field-label">{{ form_label(form.motifReservation) }}</label>
          {{ form_widget(form.motifReservation, {attr: {style:'border-radius:14px;border:1.5px solid var(--border);padding:13px 14px;font-family:Outfit,system-ui,sans-serif;background:var(--bg);width:100%;min-height:90px;resize:vertical;'}}) }}
          {% for error in form.motifReservation.vars.errors %}<div class="alert">{{ error.message }}</div>{% endfor %}
        </div>

        <div class="field-block field-full">
          <label class="field-label">{{ form_label(form.statusReservation) }}</label>
          {{ form_widget(form.statusReservation, {attr: {style:'border-radius:14px;border:1.5px solid var(--border);padding:13px 14px;font-family:Outfit,system-ui,sans-serif;background:var(--bg);width:100%;cursor:pointer;'}}) }}
          {% for error in form.statusReservation.vars.errors %}<div class="alert">{{ error.message }}</div>{% endfor %}
        </div>
      </div>

      <div class="actions">
        {{ form_widget(form.submit, {attr: {class:'btn-primary'}}) }}
        <a href="{{ path('reservation_local_front_index') }}" class="btn-secondary">
          <svg width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 12L6 8l4-4" stroke-linecap="round" stroke-linejoin="round"/></svg>
          Retour aux réservations
        </a>
      </div>

      {{ form_end(form) }}
    </section>

    <aside class="panel-card">
      <div class="section-title">📄 Récapitulatif</div>
      <div class="recap-item" style="background:#F0F8F2;">
        <div class="recap-label">Prénom</div>
        <div class="recap-value">{{ form.prenomCl.vars.value ?: '—' }}</div>
      </div>
      <div class="recap-item" style="background:#F0F8F2;">
        <div class="recap-label">Nom</div>
        <div class="recap-value">{{ form.nomCl.vars.value ?: '—' }}</div>
      </div>
      <div class="recap-item" style="background:#EBF4FF;">
        <div class="recap-label">Tarif horaire de base</div>
        <div class="recap-value">80 DT / heure</div>
      </div>
      <div class="recap-item" style="background:#EBF4FF;">
        <div class="recap-label">Supplément</div>
        <div class="recap-value">+ 20 DT par 30 min sup.</div>
      </div>
      <div style="margin-top:18px;background:#EFFAF4;border-radius:18px;padding:16px;">
        <strong style="font-size:.85rem;color:var(--g1);">Astuce :</strong><br>
        <span style="font-size:.83rem;color:var(--text-sec);line-height:1.6;">Choisissez le local ou la salle en premier, puis sélectionnez votre créneau horaire pour voir le prix se calculer automatiquement.</span>
      </div>
    </aside>
  </div>

</div>{# /page-shell #}

{# ══ FOOTER (identique reservation index) ══ #}
<footer class="footer">
  <div class="footer-inner">
    <div class="footer-top">
      <div>
        <div style="display:flex;align-items:center;gap:11px;margin-bottom:4px">
          <div style="width:42px;height:42px;border-radius:12px;background:rgba(255,255,255,.1);border:1px solid rgba(255,255,255,.17);display:grid;place-items:center;flex-shrink:0;overflow:hidden;">
            <img src="{{ asset('images/logo.png') }}" alt="MindAura Logo" style="width:100%;height:100%;object-fit:contain;border-radius:12px;">
          </div>
          <div>
            <div class="footer-brand-name">MindAura</div>
            <div class="footer-brand-sub">Bien-être &amp; Sérénité</div>
          </div>
        </div>
        <p class="footer-tagline">Une plateforme dédiée à la santé mentale et au développement personnel, conçue avec passion par des étudiants engagés.</p>
        <div class="footer-socials">
          <a href="#" class="f-social"><svg viewBox="0 0 24 24"><path d="M18 2h-3a5 5 0 00-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 011-1h3z"/></svg></a>
          <a href="#" class="f-social"><svg viewBox="0 0 24 24"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"/><path d="M16 11.37A4 4 0 1112.63 8 4 4 0 0116 11.37z"/><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/></svg></a>
          <a href="#" class="f-social"><svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 016 6v7h-4v-7a2 2 0 00-4 0v7h-4v-7a6 6 0 016-6zM2 9h4v12H2z"/><circle cx="4" cy="4" r="2"/></svg></a>
          <a href="#" class="f-social"><svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 00-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0020 4.77 5.07 5.07 0 0019.91 1S18.73.65 16 2.48a13.38 13.38 0 00-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 005 4.77a5.44 5.44 0 00-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 009 18.13V22"/></svg></a>
        </div>
      </div>
      <div>
        <div class="footer-col-title">Navigation</div>
        <a href="{{ path('locaux_psychiatrie_front_locaux_index') }}" class="footer-link">Locaux</a>
        <a href="{{ path('salle_front_salles_index') }}"              class="footer-link">Salles</a>
        <a href="{{ path('reservation_local_front_index') }}"         class="footer-link">Mes réservations</a>
        <a href="{{ path('app_home') }}"                              class="footer-link">Accueil</a>
      </div>
      <div>
        <div class="footer-col-title">Mon Espace</div>
        <a href="{{ path('app_profil') }}"  class="footer-link">Mon profil</a>
        <a href="#"                         class="footer-link">Mes réclamations</a>
        <a href="#"                         class="footer-link">Mes rendez-vous</a>
        <a href="#"                         class="footer-link">Mes objectifs</a>
        <a href="{{ path('app_logout') }}"  class="footer-link" style="color:rgba(220,38,38,.5)">Déconnexion</a>
      </div>
      <div>
        <div class="footer-col-title">Légal</div>
        <a href="#" class="footer-link">Politique de confidentialité</a>
        <a href="#" class="footer-link">Conditions d'utilisation</a>
        <a href="#" class="footer-link">Mentions légales</a>
        <a href="#" class="footer-link">RGPD &amp; Cookies</a>
      </div>
    </div>
    <div class="footer-bottom">
      <div class="footer-copy">&copy; 2026 MindAura &mdash; Projet Intégré Développement Web. Tous droits réservés.</div>
      <div class="footer-heart">Fait avec <span>&hearts;</span> par &middot; Laifi Azer &middot;</div>
    </div>
  </div>
</footer>

<div class="toast" id="toast">
  <span>&#10003;</span><span id="toast-msg">Message envoyé !</span>
</div>

<script>
  /* Navbar scroll shadow */
  const nav = document.getElementById('main-nav');
  window.addEventListener('scroll', () => nav.classList.toggle('scrolled', window.scrollY > 20));

  /* User pill dropdown */
  (function() {
    var pillBtn  = document.getElementById('userPillBtn');
    var dropdown = document.getElementById('userDropdown');
    var wrapper  = document.getElementById('userPillWrapper');
    if (!pillBtn || !dropdown) return;
    pillBtn.addEventListener('click', function(e) {
      e.stopPropagation();
      var open = dropdown.classList.toggle('open');
      pillBtn.setAttribute('aria-expanded', open);
    });
    document.addEventListener('click', function(e) {
      if (!wrapper.contains(e.target)) {
        dropdown.classList.remove('open');
        pillBtn.setAttribute('aria-expanded', 'false');
      }
    });
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape') {
        dropdown.classList.remove('open');
        pillBtn.setAttribute('aria-expanded', 'false');
      }
    });
  })();

  /* Prix automatique selon durée */
  (function () {
    const debut = document.querySelector('[id$="_heureDebutReservation"]');
    const fin   = document.querySelector('[id$="_heureFinReservation"]');
    const prix  = document.querySelector('[id$="_prixReservation"]');
    if (!debut || !fin || !prix) return;

    const PRIX_BASE    = 80;
    const PRIX_TRANCHE = 20;

    function updatePrice() {
      const a = debut.value;
      const b = fin.value;
      if (!a || !b) { prix.value = ''; return; }
      const [h1, m1] = a.split(':').map(Number);
      const [h2, m2] = b.split(':').map(Number);
      const mins = (h2 * 60 + m2) - (h1 * 60 + m1);
      if (mins <= 0) { prix.value = ''; return; }
      let total = PRIX_BASE;
      if (mins > 60) {
        total += Math.ceil((mins - 60) / 30) * PRIX_TRANCHE;
      }
      prix.value = total.toFixed(2);
    }

    debut.addEventListener('change', updatePrice);
    fin.addEventListener('change', updatePrice);
  })();
</script>
</body>
</html>
