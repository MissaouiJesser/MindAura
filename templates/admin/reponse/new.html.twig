{% extends 'base.html.twig' %}

{% block title %}Nouvelle réponse{% endblock %}

{% block body %}
<h1>Créer une nouvelle réponse</h1>
<a href="{{ path('reponse_index') }}">Retour à la liste</a>

{% for message in app.flashes('error') %}
    <div style="color: red;">{{ message }}</div>
{% endfor %}

<form method="post">
    <label for="reclamation_id">Réclamation :</label>
    <select name="reclamation_id" id="reclamation_id" required>
        <option value="">Choisir une réclamation</option>
        {% for reclamation in reclamations %}
            <option value="{{ reclamation.id_reclamation }}" {% if selectedReclamation and reclamation.id_reclamation == selectedReclamation.id_reclamation %}selected{% endif %}>{{ reclamation.sujet_reclamation }}</option>
        {% endfor %}
    </select><br><br>

    <label for="contenu_reponse">Contenu :</label><br>
    <textarea name="contenu_reponse" id="contenu_reponse" rows="4" cols="50" required></textarea><br><br>

    <label for="id_utilisateur">ID Utilisateur :</label>
    <input type="number" name="id_utilisateur" id="id_utilisateur"><br><br>

    <label for="rate_reponse">Note :</label>
    <input type="number" step="0.01" name="rate_reponse" id="rate_reponse" value="0"><br><br>

    <label for="rate_sum">Somme des notes :</label>
    <input type="number" name="rate_sum" id="rate_sum" value="0"><br><br>

    <label for="rate_count">Nombre de notes :</label>
    <input type="number" name="rate_count" id="rate_count" value="0"><br><br>

    <button type="submit">Créer</button>
</form>
{% endblock %}